# requirements.txt — כל התלויות של CleverCheck

# ── Web Framework ────────────────────────────────────────────
flask>=3.0.0

# ── Database ─────────────────────────────────────────────────
sqlalchemy>=2.0.0
pyodbc>=5.0.0           # דרייבר ODBC ל-SQL Server

# ── ML / Embeddings ──────────────────────────────────────────
sentence-transformers>=3.0.0
torch>=2.2.0

# ── סביבה ────────────────────────────────────────────────────
python-dotenv>=1.0.0    # טעינת .env אוטומטית

# ── בדיקות ───────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0       # דוח כיסוי קוד
